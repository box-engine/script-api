{
  "name": "@box/script-api",
  "version": "0.1.0",
  "description": "",
  "types": "index.d.ts",
  "scripts": {
    "dev": "typedoc --watch --plugin typedoc-plugin-markdown --excludeInternal --entryPointStrategy expand --out docs/ --tsconfig tsconfig.json --entryPoints ./server/global.d.ts --entryPoints ./server/modules",
    "doc": "typedoc --plugin typedoc-plugin-markdown --excludeInternal --entryPointStrategy expand --out docs/ --tsconfig tsconfig.json --entryPoints ./server/global.d.ts --entryPoints ./server/modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "private": "true",
  "devDependencies": {
    "typedoc": "0.24.8",
    "typedoc-plugin-markdown": "3.15.3",
    "typedoc-plugin-missing-exports": "2.0.0",
    "typescript": "4.6.3"
  }
}
